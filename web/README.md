# quotio-lite web

Frontend for Quotio-Lite.

## Commands

```bash
corepack pnpm install
corepack pnpm dev
corepack pnpm build
corepack pnpm lint
```

By default it calls backend at `http://127.0.0.1:18417`.
Override with `VITE_API_BASE_URL` if needed.
